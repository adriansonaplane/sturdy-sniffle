import { performance } from 'node:perf_hooks'; import { generateCatacombs } from '../src/dungeon/generationPipeline.js'; import { config } from '../test/dungeon/fixtures/config.js'; for(let i=0;i<20;i++){const t=performance.now(); const r=generateCatacombs({config:config(`bench-${i}`)}); console.log(JSON.stringify({seed:`bench-${i}`,ok:r.ok,ms:+(performance.now()-t).toFixed(3)}));}
