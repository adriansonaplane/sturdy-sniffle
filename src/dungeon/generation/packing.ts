import { createRngStream } from '../determinism/rng.js';
import { realizeRoomShape } from '../spatialEmbedding.js';
import type { DungeonGenerationConfig, GridBounds, PackedRoom, PackedRoomLayout, PhysicalRoomCandidate, RoomFootprint } from '../spatialEmbedding.js';
function fp(id:string,x:number,y:number,w:number,h:number,shape:any,mask:Uint8Array):RoomFootprint{const n=mask.reduce((a,b)=>a+(b?1:0),0); return {roomId:id,origin:{x,y,level:0},width:w,height:h,shape,mask,occupiedCellCount:n,bounds:{minimumX:x,minimumY:y,maximumX:x+w-1,maximumY:y+h-1,level:0}};}
function dims(c:PhysicalRoomCandidate,r:any){const b=c.scale==='landmark'?18:c.scale==='large'?14:c.scale==='medium'?10:c.scale==='small'?8:6; return {w:b+r.intInclusive(0,c.scale==='landmark'?8:4),h:b+r.intInclusive(0,c.scale==='landmark'?8:4)};}
function pad(c:PhysicalRoomCandidate,cfg:DungeonGenerationConfig){return c.scale==='landmark'?cfg.landmarkPadding:cfg.minimumRoomPadding;}
function overlap(a:PackedRoom,b:PackedRoom){const p=Math.max(a.padding,b.padding); return !(a.footprint.bounds.maximumX+p<b.footprint.bounds.minimumX||b.footprint.bounds.maximumX+p<a.footprint.bounds.minimumX||a.footprint.bounds.maximumY+p<b.footprint.bounds.minimumY||b.footprint.bounds.maximumY+p<a.footprint.bounds.minimumY);}
function bounds(rooms:readonly PackedRoom[],m=0):GridBounds{const xs=rooms.flatMap(r=>[r.footprint.bounds.minimumX-m,r.footprint.bounds.maximumX+m]),ys=rooms.flatMap(r=>[r.footprint.bounds.minimumY-m,r.footprint.bounds.maximumY+m]); return {minimumX:Math.min(...xs),minimumY:Math.min(...ys),maximumX:Math.max(...xs),maximumY:Math.max(...ys),level:0};}
export type PackingResult={readonly ok:true;readonly layout:PackedRoomLayout;readonly metrics:Readonly<Record<string,unknown>>}|{readonly ok:false;readonly failure:{stage:'packing';code:string;message:string;attemptIndex:number;affectedIds:readonly string[];retryPermitted:boolean}};
export function packPhysicalRooms(config:DungeonGenerationConfig,candidates:readonly PhysicalRoomCandidate[]):PackingResult{let best:{layout:PackedRoomLayout;metrics:Readonly<Record<string,unknown>>;attempt:number}|undefined; for(let attempt=0;attempt<config.packingCandidateCount;attempt++){const rng=createRngStream(config.rootSeed,config.generatorVersion,'packing' as any).forAttempt(attempt); let rooms:PackedRoom[]=candidates.map(c=>{const d=dims(c,rng), real=realizeRoomShape(c.shapeFamily,d.w,d.h,rng), angle=rng.float(0,Math.PI*2), radius=Math.sqrt(rng.nextFloat01())*config.initialScatterRadius; const x=Math.round(Math.cos(angle)*radius-d.w/2), y=Math.round(Math.sin(angle)*radius-d.h/2); return {candidateId:c.id,center:{x:x+d.w/2,y:y+d.h/2},footprint:fp(c.id,x,y,d.w,d.h,real.shape,real.mask),padding:pad(c,config)};}); let iterations=0; for(;iterations<config.separationIterationLimit;iterations++){let moved=false; for(let i=0;i<rooms.length;i++)for(let j=i+1;j<rooms.length;j++)if(overlap(rooms[i]!,rooms[j]!)){const a=rooms[i]!,b=rooms[j]!,dx=b.center.x-a.center.x||((i%2)*2-1),dy=b.center.y-a.center.y||((j%2)*2-1),sx=Math.abs(dx)>=Math.abs(dy)?Math.sign(dx):0,sy=Math.abs(dx)>=Math.abs(dy)?0:Math.sign(dy); const move=(r:PackedRoom,mx:number,my:number)=>({...r,center:{x:r.center.x+mx,y:r.center.y+my},footprint:fp(r.candidateId,r.footprint.origin.x+mx,r.footprint.origin.y+my,r.footprint.width,r.footprint.height,r.footprint.shape,r.footprint.mask)}); rooms[i]=move(a,-sx,-sy); rooms[j]=move(b,sx,sy); moved=true;} if(!moved)break;} const b=bounds(rooms,config.minimumRoomPadding), dx=config.minimumRoomPadding-b.minimumX, dy=config.minimumRoomPadding-b.minimumY; rooms=rooms.map(r=>({...r,center:{x:r.center.x+dx,y:r.center.y+dy},footprint:fp(r.candidateId,r.footprint.origin.x+dx,r.footprint.origin.y+dy,r.footprint.width,r.footprint.height,r.footprint.shape,r.footprint.mask)})); const conflicts=rooms.flatMap((a,i)=>rooms.slice(i+1).filter(b=>overlap(a,b)).map(b=>`${a.candidateId}:${b.candidateId}`)); const bb=bounds(rooms,config.minimumRoomPadding), area=(bb.maximumX-bb.minimumX+1)*(bb.maximumY-bb.minimumY+1), occ=rooms.reduce((s,r)=>s+r.footprint.occupiedCellCount,0), density=+(occ/area).toFixed(6); if(conflicts.length||bb.maximumX>=config.mapBounds.width||bb.maximumY>=config.mapBounds.height||area>config.areaBudget*4)continue; const layout={rooms:rooms.sort((a,b)=>a.candidateId.localeCompare(b.candidateId)),bounds:bb,density}; const metrics={attemptIndex:attempt,separationIterations:iterations,occupiedArea:occ,boundingArea:area,density,targetDensityDifference:+Math.abs(density-config.targetPackingDensity).toFixed(6),paddingConflicts:0,overlaps:0}; if(!best||Math.abs(density-config.targetPackingDensity)<Math.abs(best.layout.density-config.targetPackingDensity))best={layout,metrics,attempt};} if(!best)return {ok:false,failure:{stage:'packing',code:'PACKING_CANDIDATES_EXHAUSTED',message:'No non-overlapping bounded packing candidate remained',attemptIndex:config.packingCandidateCount,affectedIds:candidates.map(c=>c.id),retryPermitted:false}}; return {ok:true,layout:best.layout,metrics:best.metrics};}
export function roomCenter(room:PackedRoom){return {x:room.footprint.origin.x+room.footprint.width/2,y:room.footprint.origin.y+room.footprint.height/2};}
export function paddingConflicts(rooms:readonly PackedRoom[]){const out:string[]=[]; for(let i=0;i<rooms.length;i++)for(let j=i+1;j<rooms.length;j++)if(overlap(rooms[i]!,rooms[j]!))out.push(`${rooms[i]!.candidateId}:${rooms[j]!.candidateId}`); return out;}
