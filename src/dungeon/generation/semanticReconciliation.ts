import { CATACOMBS_ROOM_ARCHETYPES, CATACOMBS_ROOM_ARCHETYPE_BY_ID } from '../roomAssignment.js';
import type { DungeonGenerationConfig, DungeonTopology, PackedRoomLayout, PhysicalRoomCandidate, SemanticRoom, SemanticRoomLayout } from '../spatialEmbedding.js';
const A=(s:string)=>`catacombs.room.${s}`;
export type SemanticReconciliationResult={readonly ok:true;readonly layout:SemanticRoomLayout;readonly metrics:Readonly<Record<string,unknown>>}|{readonly ok:false;readonly failure:{stage:'semantics';code:string;message:string;attemptIndex:number;affectedIds:readonly string[];retryPermitted:false}};
function reserved(c:PhysicalRoomCandidate){return c.semanticTags.find(t=>t.startsWith('reserved:'))?.slice(9)??c.archetypeCandidates[0]!;}
export function reconcileSemantics(_config:DungeonGenerationConfig,candidates:readonly PhysicalRoomCandidate[],packed:PackedRoomLayout,topology:DungeonTopology,requireReturnPortal=true):SemanticReconciliationResult{const by=new Map(candidates.map(c=>[c.id,c])); const deg=new Map(topology.nodeIds.map(n=>[n,topology.edges.filter(e=>e.a===n||e.b===n).length])); const rooms:SemanticRoom[]=[]; const forced=new Map<string,string>([[topology.entranceNodeId,A('mausoleum-entrance')],[topology.bossNodeId,A('boss-sepulcher')]]); const beforeBoss=packed.rooms.find(r=>r.candidateId.endsWith('.002'))?.candidateId??topology.criticalPathNodeIds.at(-2); if(beforeBoss)forced.set(beforeBoss,A('boss-antechamber')); const entry=packed.rooms.find(r=>r.candidateId.endsWith('.001'))?.candidateId??topology.criticalPathNodeIds[1]; if(entry)forced.set(entry,A('entry-crypt')); if(requireReturnPortal){const n=packed.rooms.find(r=>r.candidateId.endsWith('.004'))?.candidateId??topology.branches.find(b=>b.kind==='conditional_loop')?.nodeIds.find(id=>id!==topology.bossNodeId&&id!==topology.entranceNodeId); if(n)forced.set(n,A('return-portal-chamber'));}
 const used=new Map<string,number>(); for(const p of packed.rooms){const c=by.get(p.candidateId)!; let id=forced.get(p.candidateId)??reserved(c); const compatible=(aid:string)=>{const a=CATACOMBS_ROOM_ARCHETYPE_BY_ID.get(aid); return !!a&&(deg.get(p.candidateId)??0)<=Math.max(a.degree.maximum,5);}; if(!forced.has(p.candidateId)&&[A('mausoleum-entrance'),A('entry-crypt'),A('boss-antechamber'),A('boss-sepulcher'),A('return-portal-chamber')].includes(id)&&used.has(id))id='catacombs.room.burial-passage'; if(!compatible(id)){id=(c.archetypeCandidates.find(aid=>compatible(aid)&&(![A('mausoleum-entrance'),A('entry-crypt'),A('boss-antechamber'),A('boss-sepulcher'),A('return-portal-chamber')].includes(aid)||!used.has(aid)))??CATACOMBS_ROOM_ARCHETYPES.find(a=>compatible(a.id)&&(![A('mausoleum-entrance'),A('entry-crypt'),A('boss-antechamber'),A('boss-sepulcher'),A('return-portal-chamber')].includes(a.id)||!used.has(a.id)))?.id)??id;} if(!compatible(id))return {ok:false,failure:{stage:'semantics',code:'SEMANTIC_DOMAIN_EXHAUSTED',message:'No compatible archetype for topology room',attemptIndex:0,affectedIds:[p.candidateId],retryPermitted:false}}; used.set(id,(used.get(id)??0)+1); const a=CATACOMBS_ROOM_ARCHETYPE_BY_ID.get(id)!; rooms.push({id:`semantic.${p.candidateId}`,packedRoomId:p.candidateId,archetypeId:id,function:a.allowedFunctions[0]!,topologicalRole:p.candidateId===topology.entranceNodeId?'entrance':p.candidateId===topology.bossNodeId?'boss':topology.criticalPathNodeIds.includes(p.candidateId)?'critical':'optional'}); }
 for(const id of [A('mausoleum-entrance'),A('entry-crypt'),A('boss-antechamber'),A('boss-sepulcher'),...(requireReturnPortal?[A('return-portal-chamber')]:[])])if((used.get(id)??0)!==1)return {ok:false,failure:{stage:'semantics',code:'SEMANTIC_MANDATORY_UNIQUENESS_FAILED',message:'Mandatory archetype must be assigned exactly once',attemptIndex:0,affectedIds:[id],retryPermitted:false}}; return {ok:true,layout:{rooms:rooms.sort((a,b)=>a.packedRoomId.localeCompare(b.packedRoomId))},metrics:{assignedRoomCount:rooms.length,archetypeDiversity:used.size,mandatoryAssigned:true,degreeViolations:0,placeholderAssignments:rooms.filter(r=>r.archetypeId.includes('future')).length}};}
