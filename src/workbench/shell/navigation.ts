export const PRIMARY_NAV=[['#/dungeon-generator','Dungeon Generator'],['#/asset-forge','Asset Forge'],['#/settings','Settings'],['#/about','About']] as const;
export function renderPrimaryNavigation(activeHash:string){ const nav=document.createElement('nav'); nav.className='primary-nav'; nav.setAttribute('aria-label','Primary workbench navigation'); for(const [href,label] of PRIMARY_NAV){ const a=document.createElement('a'); a.href=href; a.textContent=label; const active=activeHash===href || (href==='#/asset-forge'&&activeHash.startsWith('#/asset-forge')); a.setAttribute('aria-current',active?'page':'false'); nav.append(a); } return nav; }
