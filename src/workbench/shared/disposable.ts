export class DisposableStack { #done=false; #items: Array<()=>void|Promise<void>>=[]; defer(fn:()=>void|Promise<void>){ if(this.#done){ void fn(); return; } this.#items.push(fn); } async dispose(){ if(this.#done)return; this.#done=true; const errors:unknown[]=[]; for(const fn of this.#items.splice(0).reverse()){ try{ await fn(); }catch(e){ errors.push(e); } } if(errors.length) throw new AggregateError(errors,'DisposableStack disposal failed'); } }
