import type { AssetBudget } from '../contracts/authoritative.js';import type { AssetForgeDiagnostic } from '../core/diagnostics.js';import { diagnostic } from '../core/diagnostics.js';import type { Result } from '../core/results.js';import { ok,err } from '../core/results.js';
export const DEFAULT_ASSET_BUDGET:AssetBudget={components:256,hierarchyDepth:16,geometryOperations:256,vertices:200000,indices:600000,instances:10000,bones:128,skinInfluences:4,animationClips:32,animationTracks:512,animationKeys:20000,animationDurationSeconds:600,colliders:128,joints:64,materials:128,textures:64,textureDimension:4096,textureDecodedBytes:134217728,lods:8,externalDependencies:64,packageBytes:52428800,workUnits:1000000,diagnostics:256,inputDepth:12,collectionSize:1024};
export class AssetBudgetReservation{readonly limits:AssetBudget;#used:Partial<Record<keyof AssetBudget,number>>={};constructor(limits:Partial<AssetBudget>={}){this.limits={...DEFAULT_ASSET_BUDGET,...limits};}reserve(key:keyof AssetBudget,amount:number):Result<void,AssetForgeDiagnostic>{const next=(this.#used[key]??0)+amount;if(!Number.isFinite(amount)||amount<0||next>this.limits[key])return err(diagnostic('AF_INVALID_BUDGET',`${key} budget exceeded`,'budget',key,{limit:this.limits[key],requested:next}));this.#used[key]=next;return ok(undefined);}snapshot(){return Object.freeze({...this.#used});}}
