export type CancellationReasonCode='USER_REQUESTED'|'TIMEOUT'|'BUDGET_EXHAUSTED'|'VALIDATION_BLOCKED';export class AssetForgeCancellationContext{#cancelled=false;#reason?:CancellationReasonCode;constructor(readonly id:string){}cancel(reason:CancellationReasonCode='USER_REQUESTED'){if(!this.#cancelled){this.#cancelled=true;this.#reason=reason;}return this.snapshot();}get cancelled(){return this.#cancelled}get reason(){return this.#reason}snapshot(){return Object.freeze({id:this.id,cancelled:this.#cancelled,reason:this.#reason});}}export function cancellationFromAbortSignal(id:string,signal:AbortSignal){const c=new AssetForgeCancellationContext(id);if(signal.aborted)c.cancel('USER_REQUESTED');signal.addEventListener('abort',()=>c.cancel('USER_REQUESTED'),{once:true});return c;}
