import{canonicalKey}from'../core/identifiers.js';import{diagnostic,type AssetForgeDiagnostic}from'../core/diagnostics.js';import type{Result}from'../core/results.js';import{ok,err}from'../core/results.js';import{deepFreeze}from'../core/immutable.js';import type{MaterialProfile}from'../contracts/materialProfile.js';export class MaterialProfileRegistry{#items=new Map<string,MaterialProfile>();register(p:MaterialProfile):Result<void,AssetForgeDiagnostic>{const k=canonicalKey(p.id,p.version);if(this.#items.has(k))return err(diagnostic('AF_DUPLICATE_REGISTRATION','duplicate material profile rejected','registry',k));this.#items.set(k,deepFreeze({...p}));return ok(undefined);}snapshot(){return deepFreeze([...this.#items.values()].sort((a,b)=>canonicalKey(a.id,a.version).localeCompare(canonicalKey(b.id,b.version),'en-US-u-kf-upper')));}}
