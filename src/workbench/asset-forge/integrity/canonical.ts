import { createHash } from 'node:crypto';import { normalizeSafeJson, canonicalJson } from '../core/serialization.js';import type { Result } from '../core/results.js';import { ok,err } from '../core/results.js';import { diagnostic,type AssetForgeDiagnostic } from '../core/diagnostics.js';
export function canonicalize(value:unknown):Result<string,AssetForgeDiagnostic>{const n=normalizeSafeJson(value);return n.ok?ok(canonicalJson(n.value)):err(n.error);}export function sha256Digest(value:unknown):Result<string,AssetForgeDiagnostic>{const c=canonicalize(value);return c.ok?ok('sha256-'+createHash('sha256').update(c.value).digest('hex')):err(c.error);}
