import{diagnostic,type AssetForgeDiagnostic}from'../core/diagnostics.js';import type{Result}from'../core/results.js';import{ok,err}from'../core/results.js';export function detectSupersessionCycle(links:ReadonlyMap<string,string|undefined>):Result<void,AssetForgeDiagnostic>{for(const start of links.keys()){const seen=new Set<string>();let cur:string|undefined=start;while(cur){if(seen.has(cur))return err(diagnostic('AF_DEPENDENCY_CYCLE','supersession cycle rejected','runtimeRegistry',start));seen.add(cur);cur=links.get(cur);}}return ok(undefined)}
