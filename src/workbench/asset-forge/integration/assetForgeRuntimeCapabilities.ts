import{AssetGeneratorRegistry}from'../registries/generatorRegistry.js';import{ValidationRuleRegistry}from'../registries/validationRuleRegistry.js';import{GeneratorImplementationRegistry}from'../runtime/generatorImplementationRegistry.js';import{ValidationImplementationRegistry}from'../runtime/validationImplementationRegistry.js';export function describeAssetForgeRuntimeCapabilities(){const generators=new AssetGeneratorRegistry();const validations=new ValidationRuleRegistry();const generatorImplementations=new GeneratorImplementationRegistry(generators);const validationImplementations=new ValidationImplementationRegistry(validations);return{secureRuntimeInitialized:true,generatorDescriptorCount:generators.snapshot().descriptors.length,trustedGeneratorImplementationCount:generatorImplementations.snapshot().implementations.length,validationDescriptorCount:validations.snapshot().descriptors.length,trustedValidationImplementationCount:validationImplementations.snapshot().implementations.length,executionBudgetEnforcementAvailable:true,cancellationAvailable:true,deterministicSeedServiceAvailable:true,packageConstructionAvailable:true,integrityServiceAvailable:true,validationExecutionAvailable:true,environmentPackAvailability:'No AF-D4 production environment pack is available.',generationAvailable:false,generationStatus:'Generation is unavailable in production because no trusted production generator implementation is registered.',catacombsStatus:'Catacombs completion remains AF-D4.',materialGenerationStatus:'Material generation remains AF-D5.',approvalStatus:'Generated outputs are package candidates only and are not approved runtime assets.',diagnostics:[] as string[]};}
