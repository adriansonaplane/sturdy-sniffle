import type{AssetCategoryId,StyleProfileId}from'../core/identifiers.js';import type{ProfileVersion}from'../core/versions.js';export interface StyleProfile{readonly id:StyleProfileId;readonly version:ProfileVersion;readonly displayName:string;readonly provenanceRef:string;readonly inheritsFrom?:StyleProfileId;readonly grammar:{readonly proportions:number;readonly silhouette:number;readonly symmetry:number;readonly bevelScaleMeters:number;readonly edgeWear:number;readonly damageDensity:number;readonly surfaceAging:number;readonly ornamentDensity:number;readonly structuralRepetition:number;readonly dirtDust:number;readonly emissiveMaxNits:number;readonly colorFamilies:readonly string[];readonly materialWeights:Readonly<Record<string,number>>;};readonly categoryOverrides:Readonly<Record<AssetCategoryId,Partial<StyleProfile['grammar']>>>;}
