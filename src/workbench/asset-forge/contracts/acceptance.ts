import type{IntegrityDigest}from'../core/identifiers.js';export type AcceptanceState='DRAFT'|'GENERATED'|'VALIDATION_PENDING'|'VALIDATION_FAILED'|'VALIDATED'|'REVIEW_PENDING'|'CHANGES_REQUESTED'|'REJECTED'|'PLACEHOLDER_APPROVED'|'DEPRECATED'|'SUPERSEDED'|'REVOKED'|'PRODUCTION_APPROVED';export interface AcceptanceTransitionRecord{readonly commandId:string;readonly subjectId:string;readonly subjectDigest:IntegrityDigest;readonly from:AcceptanceState;readonly to:AcceptanceState;readonly actorRef:string;readonly authority:'AUTHORING_TOOL'|'VALIDATION_SERVICE'|'HUMAN_REVIEWER'|'GOVERNANCE_ADMIN'|'PRODUCTION_REVIEWER';readonly policyVersion:string;readonly createdAt:string;readonly reason?:string}export interface AcceptanceRecord{readonly subjectId:string;readonly subjectDigest:IntegrityDigest;readonly state:AcceptanceState;readonly validationSummaryRef?:string;readonly humanReviewRef?:string;readonly transitions:readonly AcceptanceTransitionRecord[]}
