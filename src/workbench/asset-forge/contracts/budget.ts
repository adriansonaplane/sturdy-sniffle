import{diagnostic,type AssetForgeDiagnostic}from'../core/diagnostics.js';import type{AssetBudgetProfileId}from'../core/identifiers.js';import type{BudgetProfileVersion}from'../core/versions.js';import type{Result}from'../core/results.js';import{ok,err}from'../core/results.js';import{deepFreeze}from'../core/immutable.js';
export interface AssetBudgetCeilings{readonly generationIterations:number;readonly cooperativeWorkMs:number;readonly vertices:number;readonly triangles:number;readonly indices:number;readonly meshes:number;readonly materials:number;readonly textures:number;readonly textureMaxDimensionPx:number;readonly textureMemoryBytes:number;readonly geometryMemoryBytes:number;readonly bones:number;readonly joints:number;readonly morphTargets:number;readonly anchors:number;readonly sockets:number;readonly collisionPrimitives:number;readonly lodCount:number;readonly packageBytes:number;readonly dependencies:number;readonly metadataBytes:number;readonly recipeParameterDepth:number;readonly recipeParameterEntries:number;}export interface AssetBudgetProfile{readonly id:AssetBudgetProfileId;readonly version:BudgetProfileVersion;readonly authoring:AssetBudgetCeilings;readonly runtime:AssetBudgetCeilings;}
export const CORE_ASSET_BUDGET_CEILINGS:AssetBudgetCeilings=deepFreeze({generationIterations:10000,cooperativeWorkMs:2000,vertices:100000,triangles:60000,indices:180000,meshes:64,materials:32,textures:16,textureMaxDimensionPx:2048,textureMemoryBytes:67108864,geometryMemoryBytes:33554432,bones:128,joints:128,morphTargets:16,anchors:64,sockets:64,collisionPrimitives:32,lodCount:4,packageBytes:10485760,dependencies:32,metadataBytes:65536,recipeParameterDepth:8,recipeParameterEntries:256});
export function validateBudgetCeilings(b:AssetBudgetCeilings,core:AssetBudgetCeilings=CORE_ASSET_BUDGET_CEILINGS):Result<AssetBudgetCeilings,AssetForgeDiagnostic>{for(const k of Object.keys(core) as (keyof AssetBudgetCeilings)[]){const v=b[k];if(!Number.isInteger(v)||v<0||v>core[k])return err(diagnostic('AF_INVALID_BUDGET','budget limit must be finite integer and may not weaken core ceilings','budget',String(k),{limit:String(v)}));}return ok(deepFreeze({...b}));}
export function composeStricterBudgets(a:AssetBudgetCeilings,b:AssetBudgetCeilings):AssetBudgetCeilings{const out={} as Record<keyof AssetBudgetCeilings,number>;for(const k of Object.keys(a) as (keyof AssetBudgetCeilings)[])out[k]=Math.min(a[k],b[k]);return deepFreeze(out as AssetBudgetCeilings);}
