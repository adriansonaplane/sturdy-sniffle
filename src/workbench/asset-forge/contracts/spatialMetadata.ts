import{diagnostic,type AssetForgeDiagnostic}from'../core/diagnostics.js';import type{Result}from'../core/results.js';import{ok,err}from'../core/results.js';import{deepFreeze}from'../core/immutable.js';
export type Vec3=readonly[number,number,number];export type Quat=readonly[number,number,number,number];export interface Transform3D{readonly translation:Vec3;readonly rotation:Quat;readonly scale:Vec3;}export interface Bounds3D{readonly min:Vec3;readonly max:Vec3;readonly kind:'AABB'|'OBB';}export type PlacementSurface='FLOOR'|'WALL'|'CEILING'|'CORNER'|'DOORWAY'|'ARCHWAY'|'COLUMN'|'PLATFORM'|'FREE_STANDING'|'ATTACHED'|'EMBEDDED';export interface SpatialMetadata{readonly coordinateSystem:{readonly unitScaleMeters:number;readonly handedness:'LEFT'|'RIGHT';readonly upAxis:'X'|'Y'|'Z';readonly forwardAxis:'X'|'Y'|'Z'|'-X'|'-Y'|'-Z'};readonly bounds:Bounds3D;readonly pivot:'CENTER'|'GROUND_CENTER'|'AUTHOR_DEFINED';readonly groundContact:boolean;readonly placementSurface:PlacementSurface;readonly snapIncrementMeters:number;readonly allowedRotation:'NONE'|'CARDINAL_YAW'|'FREE_YAW'|'FREE';readonly scalePolicy:'LOCKED'|'UNIFORM'|'NON_UNIFORM';readonly clearanceEnvelope?:Bounds3D;readonly occlusionEnvelope?:Bounds3D;readonly gameplayReadabilityEnvelope?:Bounds3D;}
function finite(xs:readonly number[]){return xs.every(Number.isFinite);}export function validateVec3(v:Vec3):Result<Vec3,AssetForgeDiagnostic>{return finite(v)?ok(deepFreeze([...v] as unknown as Vec3)):err(diagnostic('AF_INVALID_BOUNDS_OR_TRANSFORM','non-finite vector rejected','spatial'));}export function validateBounds(b:Bounds3D):Result<Bounds3D,AssetForgeDiagnostic>{if(!finite(b.min)||!finite(b.max)||b.min.some((x,i)=>x>b.max[i]))return err(diagnostic('AF_INVALID_BOUNDS_OR_TRANSFORM','invalid finite bounds rejected','spatial'));return ok(deepFreeze({kind:b.kind,min:[...b.min] as unknown as Vec3,max:[...b.max] as unknown as Vec3}));}
